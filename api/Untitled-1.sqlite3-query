-- database: ../instance/volumes/user_management.db

-- Use the ▷ button in the top right corner to run the entire file.

SELECT * FROM channels;
CREATE TABLE table_name (
    id INTEGER,
    user_id INTEGER,
    name ,
    score INTEGER
   ....
);